{
  "name": "simple-neon-proxy",
  "version": "1.1.0",
  "description": "Blazing fast Neon-compatible HTTP/WebSocket proxy for local PostgreSQL. Zero dependencies, powered by Bun.",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "start": "bun run index.ts",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "devDependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "@types/bun": "^1.2.17",
    "typescript": "^5.8.3"
  }
}
